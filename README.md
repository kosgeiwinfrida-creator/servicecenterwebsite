# Service Center Website

Scaffolded Next.js + Tailwind public pages: Home, Services, About, Contact, Book. API routes for contact and bookings return JSON success.

To run locally:

1. Install dependencies: npm install
2. Run dev server: npm run dev

